# 文档侧边栏：人工截图反馈与下一轮 TODO

日期：2026-09-08。跟踪：[Issue #37](https://github.com/benz-ai-x/dsh-md-preview/issues/37)。

## 证据与范围

本次已读取用户提供、标有 A/B 的真实页面截图。A 在会话头部「Session 日志」旁，
B 在预览面板最右侧。截图左侧被裁切，浏览器缩放与 Harness 字号设置未知；不将
截图像素当作 CSS px，也不据此断言完整对话区宽度或正文实际字号。

用户明确要求改两个图标，并征求进一步建议。本轮实施 F-01/F-02；以下其余条目
是下一轮候选，尚未修改布局、正文排版或用户文档。

## 本轮修正

- [x] **F-01 · A 改回形针入口。** 使用原生 `IconPaperclipOutline16`，关闭时提示
  「打开工作区文档」。保留入口的开合状态反馈、28px 命中区、键盘焦点和既有开合行为。
  图标表达附件/文档入口；实际功能仍是工作区文档浏览，不新增上传附件预览能力。
- [x] **F-02 · B 改 ×。** 使用原生 `IconCloseOutline16`，提示「关闭文档面板」，
  使用标准图标按钮样式。移除镜像变换，关闭动作不再向辅助技术报告“展开”状态。
  未保存守卫、Esc 关闭、关闭后焦点返回入口继续生效。

## 下一轮候选（按优先级）

| 优先级 / TODO | 截图或源码依据 | 建议改动与验收 |
|---|---|---|
| P1 · F-03 顶栏垂直对齐 | 截图中 A 中心线高于 B；当前面板头部用固定 `min-height: 72px`，对话头部包含自己的布局与标签行 | 对齐宿主操作行的中心线与头部底边；不要只把所有控件居中到整块头部。采集实际 DOM 几何后调整内部行布局，验收有/无会话标签、长标题、中英文及 125% 缩放。 |
| P1 · F-04 顶栏操作分组 | 大纲、文件夹、预览/编辑、全屏、关闭沿一条线排列，各组边界不明显 | 用间距区分「文档导航」「面孔切换」「面板操作」；组内 4px、组间 8–12px 作为起点，仍沿用原生 16px 图标与 28px 命中区。长文档名优先省略，完整名称/路径可聚焦查看；360px 面板仍保留保存、关闭。 |
| P1 · F-05 阅读宽度与字体核验 | 截图正文包含长中英文混排与大量加粗。代码已经复用平台 Markdown，阅读宽度上限为普通 760px / 最大化 940px | 用同一文档、相同字号、相同可用宽度与 Harness 对照 computed font/line-height，再决定是否调整宽度。优先核验中文长行与英文换行；正文行距、段距、标题间距继续随平台字号联动。保留文档显式加粗，避免全局压小字号来提高密度。 |
| P1 · F-06 顶部文档内容核查 | 可见首行为“强制覆盖走查草稿 BBB# AI 编程 Skill…”；`#` 位于行中，没有呈现一级标题 | 在编辑脸核查源文是否残留测试文字及缺少标题换行。若确认是测试残留，由文档所有者恢复正确内容；插件忠实渲染 Markdown。验收一份正常 `# 标题` 文档与当前原文，区分内容问题和渲染问题。 |
| P2 · F-07 分栏拖动的可发现性 | 分隔边界与小拖柄颜色接近背景；完整对话区宽度无法从裁切图判断 | 悬停/键盘焦点时加强分隔线与拖柄反馈，并提示可拖动。评估加入“恢复默认宽度”入口，保留现有宽度记忆及双击行为。用完整视口核验聊天输入区和工具详情，避免按裁切图改变默认比例。 |
| P2 · F-08 长文档定位与表格阅读 | 当前阅读长报告，大纲只以图标呈现；表格中长英文名称换行较多 | 区分“大纲”“工作区文档”提示；在空间允许时评估可固定的大纲与当前位置反馈，沿用现有导航偏好。核验表格、代码只在自身横向滚动，窄屏不压缩整页字号、不制造页面横向溢出。 |

执行顺序建议：F-03 → F-04 → F-05；F-06 独立核查文档内容；F-07/F-08 随长文档验收评估。

## 人工复验

- [ ] A 显示回形针、B 显示 ×；悬停提示与动作一致。
- [ ] A 开合、B 关闭、Esc 关闭均可用；脏草稿先确认，取消关闭继续编辑。
- [ ] 关闭后焦点返回 A；重新打开可继续浏览文件。
- [ ] 完整视口下记录浅/深主题及 100%/125%/200% 缩放结果；剩余验收沿用
  [侧边栏验证记录](../verification/sidebar-docking/WALKTHROUGH.md)。
