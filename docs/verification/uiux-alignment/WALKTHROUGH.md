# UI/UX 对齐实施记录（#36）

> 后续发布：本文记录 v0.9.0 源码归档阶段的证据；相关改动已随
> [v0.10.0](../releases/v0.10.0/WALKTHROUGH.md) 发布到 npm，最新状态见
> [TODO](../../../TODO.md)。下文测试数量、摘要与“本轮未发布”保留当时事实。

日期：2026-09-08。范围：[UX-01–12](../../research/harness-uiux-alignment.md)。
状态：**UX-01–11 代码已实施；自动化及打包检查通过；UX-12 实际页面验收未完成。**
本轮源码归档标签：`v0.9.0`。浏览器验收仍待完成，npm 包未在本轮发布。

## 实施决定

| TODO | 最终实现 |
| --- | --- |
| UX-01 | 替换未定义颜色变量及错误前缀；分段选中使用平台配套前景/背景；hover、当前文档、焦点、禁用态分开呈现。必要文字使用 primary/secondary，搜索命中使用加粗下划线。 |
| UX-02/03 | 导航 14px/20px，路径和状态 12px/18px，常态 400/当前文档 500；文件行 32px、目录行 34px、名称加路径行至少 48px。常规导航外边距 12px、行内 8px、图文间距 6px、分组间距 12px。 |
| UX-04 | 复用 primitives 的目录、箭头、搜索、刷新、关闭、更多等图标；平台没有的文档图标统一到本包 DocumentIcon。常用图标 16px、按钮 28px、目录展开器 24px、搜索框 32px。 |
| UX-05 | 名称与父目录独立成行，父目录最多两行；完整原始路径通过 Tooltip 和可访问名称保留。仅用 owning session 的 cwd 缩短展示路径；打开、保存、记录键不做替换。头部保留文件名，420px 以下隐藏父目录和装饰图标。 |
| UX-06 | 头部保持单行，面板操作固定在末端，560px 以下低频操作进入更多菜单。放大状态也依据实际视口宽度折叠。查找计数移入状态栏，避免挤占头部。保留接手时已有的顶部停靠改动，移除废弃的头部测量订阅并同步契约。 |
| UX-07 | 搜索固定在浏览区顶部，快捷入口/继续阅读/树/搜索结果在下方独立滚动；输入查询时隐藏快捷来源，清空恢复。导航初始宽度 220px，仅影响没有手动偏好的用户；120–320px 手动范围不变，窄导航缩减边距和装饰搜索图标。 |
| UX-08 | CodeMirror 使用平台代码字体、内容字号/行高变量、主题沟槽、光标、选区、活动行及搜索匹配颜色；Markdown 语法高亮也使用主题变量。新增构建期依赖 @lezer/highlight 1.2.3，打包内联，未增加平台运行时 external。 |
| UX-09 | 继续使用 MarkdownText 的正文、标题和段距；去掉放大档对段距/标题上距的覆盖。阅读宽度保留 760/940px，放大档只扩大阅读宽度和外部留白。 |
| UX-10 | 面板与弹层使用平台投影、边框及背景；去掉只显示版本号的底栏，浏览标题与文档身份均可聚焦查询版本；编辑保存状态仍常驻。 |
| UX-11 | 稳定滚动条占位；窄宽度和放大状态按实际宽度响应；大纲弹层靠面板右缘避免窄屏越界；搜索结果使用单一 Tab 入口，支持方向键移动和完整路径焦点提示。真实缩放和像素布局仍待 UX-12。 |

行为契约已同步至 `docs/agent/PROJECT_CONTRACT.md`，中英文 README 已更新。
未改 Host 的路径权威、搜索取消/限额、读写冲突守卫、阅读位置算法或 bundle 协议。

## 验证证据

- `pnpm verify`：123 项严格基线检查、25 个测试文件/286 项测试、类型检查、构建及 6 项产物检查通过。见 [verify.log](verify.log)。
- 有效回归覆盖：已知 cwd 的绝对路径只缩短展示、打开仍传原始身份；不同父目录的同名结果；键盘聚焦完整路径；放大后收窄窗口仍折叠工具栏并回退浏览换脸；导航手动宽度持久化；编辑、查找计数、保存/冲突、取消、阅读恢复及生命周期的既有测试。
- 新增 9 个路径案例、1 个绝对路径交互案例、1 个放大后收窄案例；移除已废弃的头部测量测试。总数从接手时 276 变为 286。
- 静态核对 `styles.ts`/`editor.tsx` 使用的 25 个主题变量：24 个由锁定 ui-theme 样式声明，`--dsh-content-font-size` 由平台主题 bootstrap/presenter 设置并有 14px 回退。此项不等同于真实页面 computed style 或对比度测量。
- JSX 缩进整理后的 `pnpm build && pnpm built:check` 再次通过，客户端 minified 输出内容不变。见 [final-build.log](final-build.log)。

## 构建身份与干净 profile 检查

- Harness：`a66e4702047846cdaa10c66c9d3df3951f5ea70d` / `0.1.2-rc.1`。
- 产品基点：`95675eced0eeca99289344dde15be951cd3c87fd`，加接手时未提交的顶部停靠/字号改动，再加本轮工作区改动；package 仍为 `0.9.0`，并非新发布版本。
- 优化前源码和构建保存在 `/tmp/dsh-md-preview-uiux-before`；其 `lib/client.js` SHA-256 为 `05a2513a460e2cda537353c872a4576693181b3392c3c07ba1b00f9bf359344e`，保留了接手时的未提交改动。
- 本轮 `lib/client.js` SHA-256：`312ef759caf4c6e9834547789db6a31b41b3b0ce634dc3db63e80e30bf67ebc7`。
- `pnpm pack:publishable` 产出的 `benz-ai-x-dsh-md-preview-0.9.0.tgz` SHA-256：`d74646222e395053e7c597393ff184de5a018311d0ff2d96af68d74d61c43e18`。35 个归档文件，无 source map、devDependencies 或 link:/workspace: 依赖，见 [文件清单](packed-files.txt)。
- 在隔离的 `DSH_HOME=/tmp/dsh-md-preview-uiux-home` 中使用 shipped `web` profile 模板安装上述 tarball；base/web-app 均由锁定 CLI 的安装闭包提供。profile 内通过普通包名导入 Host 与 Remote 成功，Host 的 name/apply/Config/inject 与无 default export 契约一致。
- 在 3190 启动成功：启动图 47 项，包含本插件；插件资源 HTTP 200，3,948,103 字节，包含 lazy-CJS factory、v0.9.0 和本轮新样式标识。见 [packed-boot.json](packed-boot.json)。
- 移除后重启成功：启动图 46 项，插件及配置行消失，旧资源返回 404。见 [packed-remove.json](packed-remove.json)。测试进程均已停止；未改用户的既有 profile 或服务。

以上证明真实打包、普通模块解析、CLI 组合、启动、资源服务和移除路径。
**没有通过真实浏览器操作页面，因此不证明当前 DOM 渲染、按钮命中框或完整阅读链路已验收。**

## UX-12 待完成矩阵

| 条件 | 待记录 |
| --- | --- |
| 浅色/深色 × 中文/英文 | 原生导航与面板的实际字体、行高、字重、前景/背景及控件状态截图。 |
| 面板 360/559/560/600/639/640/900px 和放大档 | 长中文/英文文件名、路径省略、搜索及保存/关闭/返回入口的命中框，头部无横向溢出。 |
| 导航 148/220/320px | 全量 5 个产出和 3 个最近阅读时搜索持续可见；同名父目录可分辨。 |
| 浏览器缩放 100%/125%/200% | 焦点、弹层、工具栏和滚动条稳定；状态条可换行，主操作可达。 |
| 同文档、相同有效正文宽度与字号设置 | Markdown 正文/标题的 computed font、行高、段距；长中文/英文、嵌套列表、宽表格、代码和公式。 |
| 完整流程 | 工作区搜索 → 打开正确同名文档 → 大纲定位 → 编辑保存并核对磁盘 → 关闭 → 继续阅读并核对位置恢复。 |

每张截图需记录构建来源、视口、面板/导航宽度、主题、语言、内容字号、缩放和 DPR。
本轮未生成优化后截图；历史 R3 图片不能替代此矩阵。

当前阻塞：浏览器工具连续两次无法与 Chrome 通信；浏览器、扩展及 native host 检查均正常。
按 Browser 技能的 chrome-troubleshooting 指引，已向用户请求打开当前配置的空白窗口以恢复连接，尚未收到答复；未自行打开或改用脚本绕过浏览器控制接口。
