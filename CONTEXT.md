# dsh-md-preview

DSH Web GUI 的 Markdown 预览插件领域:把回合产出的 Markdown 文档变成可点击、
可渲染、可安全编辑回写的工作区文档。本文件是词汇表;行为与权威边界见
`docs/agent/PROJECT_CONTRACT.md`。

## Language

### 面板与入口

**预览目标(Preview Target)**:
面板当前展示的一条「会话 × 文档路径」组合;关闭面板即为无目标。
_Avoid_: 当前文件、选中项、活动文档

**预览面板(Preview Panel)**:
停靠在对话右侧的浮层面板,以三副面孔切换呈现,无目标时不渲染。
_Avoid_: 侧边栏、抽屉、窗口

**面孔(Face)**:
预览面板的三种呈现之一:查看脸(渲染文档)、编辑脸(编辑草稿)、浏览脸
(工作区树);同面板互斥切换,浏览脸进入过一次后保持挂载。
_Avoid_: 模式、视图、标签页

**chip 行(Produced-file Chips)**:
回合结束消息下方的产出文件行;Markdown 芯片点击打开预览,其余芯片保持系统打开。
_Avoid_: 附件、文件列表

**预览文档动作(Preview-documents Action)**:
助手消息操作区的入口,列出该回合产出的 Markdown 文档。
_Avoid_: 文档按钮、菜单

**工作区浏览(Workspace Browser)**:
以会话工作区为根的目录树浏览能力;懒展开,单击可预览文件。
_Avoid_: 文件管理器(暗示增删改)、资源管理器

**浏览脸(Browse Face)**:
预览面板呈现工作区浏览的那副面孔,与查看脸、编辑脸同面板切换。
_Avoid_: 侧边栏、文件面板

**静默重验(Silent Revalidate)**:
对已展开目录的一次无感刷新:新结果到达前保留现列表,失败不改变任何内容。
进入浏览脸与点击刷新按钮都会触发。
_Avoid_: 强制刷新、重新加载(与冲突语义混淆)

**大纲(Outline)**:
一份文档的 ATX 标题序列(级别、显示文本、源行号);面板头部弹层按它导航,
查看脸定位渲染标题,编辑脸跳转源行。
_Avoid_: 目录(与文件目录混淆)、TOC

**图表增强(Diagram Pass)**:
渲染定型后,面板对**自己子树内**的 mermaid 代码块做的渲染后处理:
横幅保留(复制仍取源码),代码隐藏,SVG 就位;一切失败回退纯代码块。
_Avoid_: 图表渲染器(渲染权威仍归平台 primitive)、mermaid 插件

### 文档与读取

**可预览文档(Previewable Document)**:
扩展名在可预览并集(可编辑集的超集,默认含纯文本)内的**已存在**文档;不支持新建,非可编辑成员只读展示。
_Avoid_: Markdown 文件(未区分可预览性)、附件

**工作区(Workspace)**:
以会话工作目录为根的文件范围;一切读取与写回都不得越出此范围。
_Avoid_: 目录、文件夹、仓库

**指纹(Fingerprint)**:
一次读取时刻文件新鲜度的不透明凭据。保存必须出示有效指纹,或明确选择强制覆盖。
_Avoid_: 版本号(与插件包版本混淆)、mtime、etag、校验和

**稳定失败码(Stable Failure Code)**:
`md-preview/<reason>` 形式的机器可读失败标识;面板错误路径直接向用户展示它。
_Avoid_: 错误码(未带域前缀时)、异常

### 编辑与保存

**预览会话(Preview Session)**:
一个预览目标从打开到关闭之间的完整生命周期:内容读取、编辑会话、保存与提示。
唯一重置点是一次新的读取开始。
_Avoid_: 面板状态(指渲染,不指生命周期)、会话(与 DSH Session 混淆)

**编辑会话(Edit Session)**:
从进入编辑到保存或取消之间的一段面板状态;草稿只存活于其中。
_Avoid_: 编辑模式(指面孔,不指会话)

**草稿(Draft)**:
编辑中尚未写回工作区的内容;唯一出口是保存,离开编辑会话即消亡。
_Avoid_: 缓存、临时文件

**脏(Dirty)**:
草稿与所读内容不一致的状态;脏草稿触发关闭守卫。
_Avoid_: 已修改(与"文件被别方修改"混淆)

**保存(Save)**:
持指纹把草稿写回工作区并回到查看;成功必有可见确认。
_Avoid_: 提交、同步

**冲突(Conflict)**:
保存时发现文件自读取后已被别方(另一会话、agent、外部编辑器)修改;用户必须二选一。
_Avoid_: 竞态、冲突提示

**重新加载(Reload)**:
冲突时的选择之一:丢弃草稿,以工作区当前内容为准。
_Avoid_: 刷新、重新读取

**强制覆盖(Force Overwrite)**:
冲突时的选择之一:明示丢弃别方改动,以草稿为准写回。
_Avoid_: 忽略冲突、强制保存

**未保存守卫(Unsaved Guard)**:
带脏草稿离开预览会话前的最后一问:放弃修改,或继续编辑。
_Avoid_: 确认框、离开提示

**离开意图(Leave Intent)**:
经同一席位发出的一次离开请求(打开另一预览目标、关闭面板、切回查看脸);
脏草稿时由未保存守卫扣留首个请求,继续编辑撤销之,放弃修改原样执行一次。
_Avoid_: 导航事件、路由跳转(宿主概念)、弹窗回调

### 阅读连续

**阅读记录(Reading Record)**:
按「会话 × 文档路径」区分的阅读位置事实(章节身份、节内偏移、滚动比例、时间戳),
带版本、有效性检查与有界清理;是 UI 局部 viewing state,不持有正文、草稿或指纹。
_Avoid_: 书签(暗示用户手动创建)、阅读历史(暗示正文内容)、进度缓存

**章节身份(Section Identity)**:
一个标题的可区分身份:标题文本加同文标题中的出现序号;重复标题靠序号区分。
_Avoid_: 标题 ID(DOM 概念)、锚点(HTML 概念)

**位置恢复(Position Restore)**:
重新打开文档、经 Remote 读到最新内容并渲染稳定后,按阅读记录落回原章节附近的一次性滚动;
每次打开只应用一次,读者先行的滚动或导航取消之,失败时按有效范围回退到比例或开头。
_Avoid_: 滚动记忆(未表达一次语义)、自动定位(未表达回退链)

**偏好记忆(Preference Record)**:
面板几何与导航手动选择的记录:面板宽度、侧栏宽度、侧栏开合为全局,
文件/大纲选择按会话区分;带版本、有效性检查与有界清理,恢复值按当前视口与支持范围钳制。
空值、非法值、旧记录与存储故障一律回默认,不误落最小宽度。
_Avoid_: 设置(暗示系统配置)、布局缓存(暗示自动布局结果)

**意图安排(Intent Arrangement)**:
无手动偏好时按打开意图安排空间:直接打开具体文档优先正文(侧栏让位),
显式进入工作区浏览展开导航;任何手动选择(当次或已记忆)优先于自动安排。
_Avoid_: 智能布局(暗示猜测)、默认侧栏(与手动偏好混淆)

**阅读尺寸(Read Measure)**:
渲染正文居中呈现的阅读宽度;普通宽度与放大各有一档,长表格与代码块在各自区域内横向滚动。
_Avoid_: 页宽、全宽(未表达分档)、排版限制(未表达阅读目的)

**继续阅读入口(Continue-reading Entry)**:
工作区浏览起始区域的一条显式入口,标明本会话最近阅读的文档;
点击经统一离开意图打开该目标并复用位置恢复,无记录时不展示。
_Avoid_: 上次文档按钮(暗示暗中替换)、「工作区文档」的别名(语义不同)
