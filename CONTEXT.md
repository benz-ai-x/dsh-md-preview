# dsh-md-preview

DSH Web GUI 的 Markdown 预览插件领域:把回合产出的 Markdown 文档变成可点击、
可渲染、可安全编辑回写的工作区文档。本文件是词汇表;行为与权威边界见
`docs/agent/PROJECT_CONTRACT.md`。

## Language

### 官方标签与入口（2026-09-09）

当前实现遵循 [ADR-0007](docs/adr/0007-markdown-in-official-sidebar-tabs.md)。以下后续章节的
多格式、独立大纲、浏览脸与持久阅读术语仍用于已接受的后置设计，不代表首阶段已接入。

**官方资源标签(Resource Tab)**：Harness 管理的会话内标签记录，引用资源地址并持有 occurrence
取消信号；分栏、浮动和隐藏不等于删除记录。正文组件可重挂载。

**预览目标(Preview Target)**：某个存活标签引用的文件资源及显式文件会话身份；不再是全局单目标。

**预览面板(Preview Panel)**：历史上指独立停靠面板。当前正文承载于官方资源标签，旧布局适配已退出。

**面孔(Face)**：一个 Markdown 标签的查看脸（渲染正文）或编辑脸（源码草稿）。
浏览脸属于后置旧浏览设计，不是当前标签的第三种面孔。

**chip 行(Produced-file Chips)**：官方回合产出文件行；插件不再接管此 chain，Markdown 走官方资源导航。

**预览文档动作(Preview-documents Action)**：保留的助手消息动作，根据 owning turn 的产出事实列出
Markdown，经官方地址工具打开同一会话资源。

**工作区浏览(Workspace Browser)**：首阶段使用官方工作区树。旧搜索/浏览组件保留供后置任务复用。

**大纲(Outline)**：文档的 ATX 标题序列；当前仅复用其源行到预览章节定位，独立 UI 与阅读恢复后置。

**图表增强(Diagram Pass)**：在自己正文子树内将 Mermaid 围栏增强为 SVG，复制仍保留源码；
失败回退代码，取消后不再写 DOM。

### 文档与读取

**可预览文档(Previewable Document)**:
已存在并符合读取条件、可在预览面板查看或播放的工作区文件;
是否可编辑是独立资格。
_Avoid_: Markdown 文件(未区分可预览性)、附件

**预览美化(Preview Presentation)**:
文本文档在查看脸中的可读性呈现,包括语法配色、行号、折叠和适合文件格式的展示排版。
美化只影响显示,不改变工作区原文件的内容。
_Avoid_: 代码美化(未区分显示与写回)、格式化保存(属于文件修改)

**格式化预览(Formatted Preview)**:
JSON、XML 等文本文档按结构缩进、换行并配合语法高亮的只读呈现,
使紧凑内容便于阅读;展示保持原有信息,不改写工作区原文件。
_Avoid_: 语法高亮(仅为呈现的一部分)、格式化保存(属于文件修改)

**纯文本回退(Plain-text Fallback)**:
文本文档的语言未识别或无法完成格式化时,以原始文本供阅读的保底呈现。
_Avoid_: 不支持文件(文本仍可阅读)、编辑器(此呈现只读)

**JSONL 记录预览(JSONL Record Preview)**:
JSONL 在查看脸中的逐条记录呈现,每条记录可展开或折叠,展开内容以缩进和语法配色阅读,
也可切换为原始文本呈现。
_Avoid_: JSON 数组预览(混淆文件结构)、表格预览(暗示按列组织记录)

**分段文本预览(Incremental Text Preview)**:
先呈现部分内容,再由读者请求读取后续内容的文本阅读方式;已加载范围和是否读完明确可辨。
_Avoid_: 完整预览(尚未读取全部)、截断预览(暗示无法继续读取)

**继续读取(Load More)**:
在当前分段文本预览中请求下一段尚未加载内容的操作。
_Avoid_: 继续阅读(打开最近阅读文档的入口)、刷新(重新读取已有内容)

**内容刷新(Content Refresh)**:
读者在已有预览目标上主动重新读取工作区文件,以最新内容更新只读呈现的操作。
_Avoid_: 继续读取(加载后续文本)、静默重验(刷新目录列表)、重新加载(编辑冲突时丢弃草稿)

**图片预览(Image Preview)**:
在预览面板内以图像呈现文件的只读查看方式,可缩放、平移及按原始尺寸查看,
动图保留动画;查看操作不改变工作区原文件。
_Avoid_: 图片编辑器、静态截图预览(不能表示动图)

**内嵌浏览器预览(Embedded Browser Preview)**:
以本地 HTML 文件为入口的只读页面预览,在面板内保留浏览器中的页面显示与交互体验,
包含页面自身的样式、脚本交互和常规联网内容;只读指不编辑或保存 HTML 源文件。
_Avoid_: HTML 源码预览(只表示阅读代码)、静态截图预览

**HTML 关联文件(HTML-linked File)**:
本地 HTML 为呈现内容或交互而引用的同一会话工作区内的文件,如样式、脚本、图片和数据。
关联文件可与 HTML 分处工作区内的不同目录。
_Avoid_: 同目录文件(范围过窄)、附件(不是用户上传物)

**HTML 链接导航(HTML Link Navigation)**:
读者通过预览 HTML 页面中的链接打开目标文档或网站的行为。
_Avoid_: HTML 关联文件加载(为页面读取资源)、联网请求(不一定打开另一页面)

**工作区(Workspace)**:
以会话工作目录为根的文件范围;一切读取与写回都不得越出此范围。
_Avoid_: 目录、文件夹、仓库

**指纹(Fingerprint)**:
一次读取时刻文件新鲜度的不透明凭据。保存必须出示有效指纹,或明确选择强制覆盖。
_Avoid_: 版本号(与插件包版本混淆)、mtime、etag、校验和

**稳定失败码(Stable Failure Code)**:
`md-preview/<reason>` 形式的机器可读失败标识;面板错误路径直接向用户展示它。
_Avoid_: 错误码(未带域前缀时)、异常

### 编辑与保存

**预览会话(Preview Session)**:
一个预览目标从打开到关闭之间的完整生命周期:内容读取、编辑会话、保存与提示。
唯一重置点是一次新的读取开始。
_Avoid_: 面板状态(指渲染,不指生命周期)、会话(与 DSH Session 混淆)

**编辑会话(Edit Session)**:
从进入编辑到保存或取消之间的一段面板状态;草稿只存活于其中。
_Avoid_: 编辑模式(指面孔,不指会话)

**草稿(Draft)**:
编辑中尚未写回工作区的内容;唯一出口是保存,离开编辑会话即消亡。
_Avoid_: 缓存、临时文件

**脏(Dirty)**:
草稿与所读内容不一致的状态;脏草稿触发关闭守卫。
_Avoid_: 已修改(与"文件被别方修改"混淆)

**保存(Save)**:
持指纹把草稿写回工作区并回到查看;成功必有可见确认。
_Avoid_: 提交、同步

**冲突(Conflict)**:
保存时发现文件自读取后已被别方(另一会话、agent、外部编辑器)修改;用户必须二选一。
_Avoid_: 竞态、冲突提示

**重新加载(Reload)**:
冲突时的选择之一:丢弃草稿,以工作区当前内容为准。
_Avoid_: 刷新、重新读取

**强制覆盖(Force Overwrite)**:
冲突时的选择之一:明示丢弃别方改动,以草稿为准写回。
_Avoid_: 忽略冲突、强制保存

**未保存守卫(Unsaved Guard)**:
带脏草稿离开预览会话前的最后一问:放弃修改,或继续编辑。
_Avoid_: 确认框、离开提示

**离开意图(Leave Intent)**:
经同一席位发出的一次离开请求(打开另一预览目标、关闭面板、切回查看脸);
脏草稿时由未保存守卫扣留首个请求,继续编辑撤销之,放弃修改原样执行一次。
_Avoid_: 导航事件、路由跳转(宿主概念)、弹窗回调

### 阅读连续

**阅读记录(Reading Record)**:
按「会话 × 文档路径」区分的阅读位置事实(章节身份、节内偏移、滚动比例、时间戳),
带版本、有效性检查与有界清理;是 UI 局部 viewing state,不持有正文、草稿或指纹。
_Avoid_: 书签(暗示用户手动创建)、阅读历史(暗示正文内容)、进度缓存

**章节身份(Section Identity)**:
一个标题的可区分身份:标题文本加同文标题中的出现序号;重复标题靠序号区分。
_Avoid_: 标题 ID(DOM 概念)、锚点(HTML 概念)

**位置恢复(Position Restore)**:
重新打开文档、经 Remote 读到最新内容并渲染稳定后,按阅读记录落回原章节附近的一次性滚动;
每次打开只应用一次,读者先行的滚动或导航取消之,失败时按有效范围回退到比例或开头。
_Avoid_: 滚动记忆(未表达一次语义)、自动定位(未表达回退链)

**偏好记忆(Preference Record)**:
面板几何与导航手动选择的记录:面板宽度、侧栏宽度、侧栏开合为全局,
文件/大纲选择按会话区分;带版本、有效性检查与有界清理,恢复值按当前视口与支持范围钳制。
空值、非法值、旧记录与存储故障一律回默认,不误落最小宽度。
_Avoid_: 设置(暗示系统配置)、布局缓存(暗示自动布局结果)

**意图安排(Intent Arrangement)**:
无手动偏好时按打开意图安排空间:直接打开具体文档优先正文(侧栏让位),
显式进入工作区浏览展开导航;任何手动选择(当次或已记忆)优先于自动安排。
_Avoid_: 智能布局(暗示猜测)、默认侧栏(与手动偏好混淆)

**阅读尺寸(Read Measure)**:
渲染正文居中呈现的阅读宽度;普通宽度与放大各有一档,长表格与代码块在各自区域内横向滚动。
_Avoid_: 页宽、全宽(未表达分档)、排版限制(未表达阅读目的)

**继续阅读入口(Continue-reading Entry)**:
工作区浏览起始区域的一条显式入口,标明本会话最近阅读的文档;
点击经统一离开意图打开该目标并复用位置恢复,无记录时不展示。
_Avoid_: 上次文档按钮(暗示暗中替换)、「工作区文档」的别名(语义不同)

### 查找高效

**工作区文档搜索(Workspace Document Search)**:
以宿主遍历为权威、覆盖当前会话工作区全部目录(含未展开目录)的文档名称搜索;
名称不区分大小写子串匹配,结果带名称与工作区相对路径,不读取正文、不建索引;
查询显式携带会话身份与取消信号。
_Avoid_: 文件筛选(旧能力,只扫已展开节点)、全文搜索、编辑器内查找(作用域是文档正文)

**结果不完整(Incomplete Results)**:
搜索因目录读取失败或达到遍历/结果上限而未能覆盖整个工作区时的诚实状态表达:
保留已得结果并说明原因与下一步;与「没有结果」(仅完整搜索且零命中)严格区分。
_Avoid_: 无匹配(完整语义)、搜索失败(检索本身出错,是另一种状态)

**快捷入口(Quick Entries)**:
工作区浏览起始区域的两小节少量直接入口——「当前回合产出」(最新回合的可预览产出文档)
与「最近阅读」(本会话阅读记录的近期文档);每行名称加必要路径,打开走统一守卫路径。
_Avoid_: 收藏(暗示用户手动创建)、置顶(暗示排序操作)、文件列表(暗示完整范围)

**当前回合产出(Current-turn Outputs)**:
owning service 回合产出事实的最新回合可预览文档视图:以时间线最新回合为界、
关闭回合按关闭序号截断(与 chip 行同一边界),进行中回合取已产出部分;不解析对话正文。
_Avoid_: 本轮文件(未表达回合边界)、最近产出(暗示跨回合)
